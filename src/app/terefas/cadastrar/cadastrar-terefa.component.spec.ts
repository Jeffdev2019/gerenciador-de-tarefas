import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CadastrarTerefaComponent } from './cadastrar-terefa.component';

describe('CadastrarTerefaComponent', () => {
  let component: CadastrarTerefaComponent;
  let fixture: ComponentFixture<CadastrarTerefaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CadastrarTerefaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    /*fixture = TestBed.createComponent(CadastrarTerefaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();*/
  });
});
