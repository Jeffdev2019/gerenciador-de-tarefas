import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EditarTerefaComponent } from './editar-terefa.component';

describe('EditarTerefaComponent', () => {
  let component: EditarTerefaComponent;
  let fixture: ComponentFixture<EditarTerefaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EditarTerefaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    /*fixture = TestBed.createComponent(EditarTerefaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();*/
  });

});
