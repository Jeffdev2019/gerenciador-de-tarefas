import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListarTerefaComponent } from './listar-terefa.component';

describe('ListarTerefaComponent', () => {
  let component: ListarTerefaComponent;
  let fixture: ComponentFixture<ListarTerefaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListarTerefaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    /*fixture = TestBed.createComponent(ListarTerefaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();*/
  });
});
