import { TerefaConcluidaDirective } from './terefa-concluida.directive';

describe('TerefaConcluidaDirective', () => {
  /*it('should create an instance', () => {
    const directive = new TerefaConcluidaDirective();
    expect(directive).toBeTruthy();
  });*/
});
